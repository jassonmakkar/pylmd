Metadata-Version: 2.4
Name: pyLMD
Version: 0.1.0
Summary: GPU-accelerated Localized Marker Detector (LMD) for Python
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: networkx>=2.8
Requires-Dist: typing-extensions>=4.6
Provides-Extra: gpu
Requires-Dist: cupy-cuda11x; platform_system != "Windows" and extra == "gpu"
Requires-Dist: cuml-cu11; platform_system != "Windows" and extra == "gpu"
Requires-Dist: cugraph-cu11; platform_system != "Windows" and extra == "gpu"

# pylmd (Python)

GPU-accelerated reimplementation of Localized Marker Detector (LMD) for Python.

- Input: AnnData object
- Acceleration: tries RAPIDS if GPU available; falls back to NumPy/SciPy/Scikit-learn

Install

```
# CPU-only
pip install -e pylmd

# With GPU acceleration (RAPIDS)
pip install -e pylmd[gpu]
```

Python Usage

```python
from pylmd.lmd import lmd

res = lmd(adata, n_neighbors=30, use_rep='X_pca', correction=False)
# res contains 'score_profile', 'lmds', 'rank', 'knee_index'
```
